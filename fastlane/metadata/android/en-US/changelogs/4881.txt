- Terminal: tmux/screen sessions run with the alternate screen disabled (for native scrollback) now reflow when the soft keyboard opens, instead of shifting the view up. The top status row — e.g. tmux's scroll/copy-mode indicator — no longer drops off the top of the screen. (#206)
- Cloud storage: rclone providers that use credentials instead of a browser sign-in (Filen, plus S3, B2, MEGA, SFTP, WebDAV, FTP) can now be configured in-app — pick the provider, fill in the account fields, and tap Configure to save and verify the remote. Adds Filen and bumps bundled rclone to 1.73.5. (#181)
